({
  doInit: function (component, event, helper) {
    //Get the userType of the user to see if authenticated or not
    //Then generate cards from data in Helper based on userType
    helper.init(component); 
  }
});